import 'package:flutter/material.dart';

class SearchMechanicInputScreen extends StatelessWidget {
  const SearchMechanicInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}